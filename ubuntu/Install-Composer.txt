Install Composer to Ubuntu
------------------------------
curl -sS https://getcomposer.org/installer | php
sudo mv composer.phar /usr/local/bin/composer
sudo chmod +x /usr/local/bin/composer


----------------
# Go to specific folder: /usr/local/bin/composer and run command:
# For See version

sudo composer --version

# again close terminal and go to any folder or desktop for check globally work or not composer and agin give comman:

sudo composer --version

-------------- I hope now ok...... :)
